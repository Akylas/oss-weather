Place your weather icon theme folders here (e.g. meteocons/images/800d.png).
These assets are merged into the app APK and are accessible via context.assets during
Glance Compose preview and at runtime when the icon is referenced by relative path.
